export class Environment {
  public telegram = {
    API_URL: "",
    API_KEY: ""
  };

  public slack = {
    INCOMING_WEBHOOK_URL: "",
  };

  public webServer = {
    WEBSERVER_PORT: 3000,
  };

  public root = {
    API_URL: "",
    API_KEY: "https://sandbox.root.co.za"
  };

  public dialogFlow = {
    ACCESS_TOKEN: "",
  };
}